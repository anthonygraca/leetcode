#ifndef LEETCODE_EASY_270_CLOSEST_BINARY_TREE_VALUE_H_
#define LEETCODE_EASY_270_CLOSEST_BINARY_TREE_VALUE_H_

/*
  Given a non-empty binary search tree and a target value, find the value in 
  the BST that is closest to the target.

   Note:

   Given target value is a floating point.

   You are guaranteed to have only one unique value in the BST that is 
   closest to the target.
 */

#include "util/tree-node.h"

namespace leetcode {
class ClosestBinaryTreeValue {
public:
  int closestValue(leetcode::TreeNode* root, float target) {
    return 3;
  }
}; 
} // namespace

#endif
